{% extends "layout.template" %}

{% block title %}{{ entry.title }} - {{ blog.name }}{% endblock %}

{% block content %}
<<<<<<< HEAD
  <div class="title">
    <h1>{{ entry.title }}</h1>
  </div>
  <p>{{ entry.content }}</p>
  <div>
    <span class="badge badge-important">Posted {{ entry.published.strftime('%B %d, %Y') }}</span>

    <div class="pull-right">
    {% for category in entry.categories %}
      <a href="/category/{{ category }}"><span class="badge badge-info">{{ category }}</span></a>
    {% endfor %}
    </div>
  </div>
</div>

<hr>

<div id="disqus_thread"></div>
{% endblock %}

{% set modules = ['prettify'] %}
{% block ready %}
window.disqus_shortname = '{{ blog.disqus_shortname }}'

setTimeout(function() {
  $.ajax({
    type : "GET",
    url : ('http://' + disqus_shortname + '.disqus.com/embed.js'),
    dataType : "script",
    cache : true
  })
}, 1000)
{% endblock %}

